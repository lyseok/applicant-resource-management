<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.recruitment.applicant.applicant">

<insert id="">
	INSERT INTO APPLICANT 
		(APPLICANT_ID
			, RECRUITMENT_NO
			, RESUME_NO
			, RESUME_VIEW_YN
			, USER_ID
			, STATUS) 
	VALUES (
		#{applicantId}
			, #{recruitmentNo}
			, #{resumeNo}
			, #{resumeViewYn}
			, #{userId}
			, #{status})
</insert>

<select id="all">
	SELECT
		 APPLICANT_ID
		 	, RECRUITMENT_NO
		 	, RESUME_NO
		 	, RESUME_VIEW_YN
		 	, USER_ID
		 	, STATUS 
		  FROM APPLICANT
</select>

<select id="one">
	SELECT 
		APPLICANT_ID
			, RECRUITMENT_NO
			, RESUME_NO
			, RESUME_VIEW_YN
			, USER_ID
			, STATUS 
		FROM APPLICANT 
		WHERE 
			APPLICANT_ID = #{applicantId}
</select>

<update id="">
	UPDATE
		 APPLICANT 
	SET 
		RECRUITMENT_NO = #{recruitmentNo}
		, RESUME_NO = #{resumeNo}
		, RESUME_VIEW_YN = #{resumeViewYn}
		, USER_ID = #{userId}
		, STATUS = #{status}
	 WHERE
	 	 APPLICANT_ID = #{applicantId}
</update>

<delete id="">
	DELETE FROM APPLICANT 
	WHERE APPLICANT_ID = #{applicantId}
</delete>
</mapper>


