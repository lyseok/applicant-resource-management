<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.recruitment.RecruitmentEducationMapper">
	<select id="selectRecruitmentEduList" resultType="RecruitmentEducationVO">
	SELECT 
		RECRUITMENT_EDUCATION_CODE
		, CODE_DETAIL_NO
		, RECRUITMENT_NO 
	FROM RECRUITMENT_EDUCATION
	</select>
	
	<select id="selectRecruitmentEdu" resultType="RecruitmentEducationVO">
	SELECT 
		RECRUITMENT_EDUCATION_CODE
		, CODE_DETAIL_NO
		, RECRUITMENT_NO 
	FROM RECRUITMENT_EDUCATION 
	WHERE RECRUITMENT_EDUCATION_CODE = #{recruitmentEducationCode}
	</select>
	
	<insert id="insertRecruitmentEducation">
	<selectKey resultType="string" keyProperty="recruitmentEducationCode" order="BEFORE">
    SELECT
        'REED'||
        LPAD(NVL(TO_NUMBER(SUBSTR(MAX(RECRUITMENT_EDUCATION_CODE), LENGTH('REED' ) + 1)), 0) + 1, 6, '0')
    FROM RECRUITMENT_EDUCATION
	</selectKey>
	INSERT INTO RECRUITMENT_EDUCATION (
		RECRUITMENT_EDUCATION_CODE
		, CODE_DETAIL_NO
		, RECRUITMENT_NO
	) VALUES (
		#{recruitmentEducationCode}
		, #{codeDetailNo}
		, #{recruitmentNo}
	)
	</insert>
	
	<update id="updateRecruitmentEducation">
	UPDATE RECRUITMENT_EDUCATION SET 
		CODE_DETAIL_NO = #{codeDetailNo}
	WHERE RECRUITMENT_EDUCATION_CODE = #{recruitmentEducationCode}
	</update>
	
	<delete id="deleteRecruitmentEducation">
	UPDATE RECRUITMENT_EDUCATION SET
	RECRUITMENT_EDU_DEL_DATE = SYSDATE 
	WHERE RECRUITMENT_EDUCATION_CODE = #{recruitmentEducationCode}
	</delete>
</mapper>