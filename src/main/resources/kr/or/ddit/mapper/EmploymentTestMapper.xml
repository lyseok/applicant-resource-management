<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.EmploymentTestMapper">
	<select id="selectListEmploymentTest">
		SELECT 
			EMPLOYMENT_ID, EMP_DETAIL, EMP_FINISHDATE, WELARE, EMP_DESK
		FROM EMPLOYMENTTEST
	</select>

</mapper>