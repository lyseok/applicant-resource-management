<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.community.ComReviewTagMapper">
	<select id="selectComReviewTagByNo">
		SELECT 
			TAG_NO, 
			COM_REVIEW_NO, 
			COM_REVIEW_TAG_DATE 
		FROM COM_REVIEW_TAG 
		WHERE TAG_NO = #{tagNo} AND COM_REVIEW_NO = #{comReviewNo}
	</select>
	
	<select id="selectComReviewTagList">
		SELECT 
			TAG_NO
			,COM_REVIEW_NO
			,TO_CHAR(COM_REVIEW_TAG_DATE, 'YYYY-MM-DD HH24:MI:SS') 
		FROM COM_REVIEW_TAG
	</select>
	
	
	<select id="searchComReviewTagList">
		SELECT 
			TAG_NO
			,COM_REVIEW_NO
			,TO_CHAR(COM_REVIEW_TAG_DATE, 'YYYY-MM-DD HH24:MI:SS')
		FROM COM_REVIEW_TAG
		WHERE COM_REVIEW_NO = #{comReviewNo}
	</select>
	
	<insert id="insertComReviewTag">
		INSERT INTO COM_REVIEW_TAG (
			TAG_NO
			,COM_REVIEW_NO
			,COM_REVIEW_TAG_DATE
		) VALUES (
			#{tagNo}, 
			#{comReviewNo}, 
			TO_CHAR(SYSDATE, 'YYYYMMDD') 
		)
	</insert>
	
	<update id="updateComReviewTag">
		UPDATE COM_REVIEW_TAG 
		SET 
		COM_REVIEW_TAG_DATE = #{comReviewTagDate} 
		WHERE TAG_NO = #{tagNo} AND COM_REVIEW_NO = #{comReviewNo}
	</update>
	
	
	

</mapper>