<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.project.TaskManagerMapper">

<select id="selectTasksManagerList">
	SELECT 
		TASK_NO
		, PRJ_NO
		, USER_ID
	FROM TASKS_MANAGER
</select>

<select id="selectTasksManagerByPk">
	SELECT 
		TASK_NO
		, PRJ_NO
		, USER_ID
	FROM TASKS_MANAGER
	WHERE TASK_NO = #{taskNo}
	AND PRJ_NO = #{prjNo}
	AND USER_ID = #{userId}
</select>

<insert id="insertTasksManager">
	INSERT INTO TASKS_MANAGER (
		TASK_NO
		, PRJ_NO
		, USER_ID
	) VALUES (
		#{taskNo}
		, #{prjNo}
		, #{userId}
	)
</insert>

<delete id="deleteTasksManager">
	DELETE FROM TASKS_MANAGER
	WHERE TASK_NO = #{taskNo}
	AND PRJ_NO = #{prjNo}
	AND USER_ID = #{userId}
</delete>

</mapper>