<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.project.PrjAnncBoardTagMapper">

<select id="selectPrjAnncBoardTagList">
	SELECT 
		TAG_NO
		, PRJ_ANNC_NO
		, TAG_DATE
	FROM PRJ_ANNC_BOARD_TAG
</select>

<select id="selectprjAnncBoardTagByPk">
	SELECT 
		TAG_NO
		, PRJ_ANNC_NO
		, TAG_DATE
	FROM PRJ_ANNC_BOARD_TAG
	WHERE TAG_NO = #{tagNo}
	AND PRJ_ANNC_NO = #{prjAnncNo}
</select>

<select id="searchPrjAnncBoardTagList">
	SELECT 
		TAG_NO
		, PRJ_ANNC_NO
		, TAG_DATE
	FROM PRJ_ANNC_BOARD_TAG
	WHERE PRJ_ANNC_NO = #{prjAnncNo}
</select>

<insert id="insertPrjAnncBoardTag">
	INSERT INTO PRJ_ANNC_BOARD_TAG (
		TAG_NO
		, PRJ_ANNC_NO
		, TAG_DATE
	) VALUES (
		#{tagNo}
		, #{prjAnncNo}
		, REPLACE(SYSDATE, '-', '')
	)
</insert>

<delete id="deletePrjAnncBoardTag">
	DELETE FROM PRJ_ANNC_BOARD_TAG
	WHERE  TAG_NO = #{tagNo}
	AND PRJ_ANNC_NO = #{prjAnncNo}
</delete>

</mapper>