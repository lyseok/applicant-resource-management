<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.common.ScrabCompanyMapper">

	<select id="selectScrabCompanyList">
		SELECT 
			USER_ID
			, COMPANY_ID
			, SCRAB_COMPANY_DATE
		FROM SCRAB_COMPANY
	</select>
	
	<select id="selectMyScrabCompanyList">
		SELECT 
			USER_ID
			, COMPANY_ID
			, SCRAB_COMPANY_DATE
		FROM SCRAB_COMPANY
		WHERE USER_ID = #{userId}
	</select>
	
	<insert id="insertScrabCompany">
		INSERT INTO SCRAB_COMPANY (
			USER_ID
			, COMPANY_ID
			, SCRAB_COMPANY_DATE
		) VALUES (
			#{userId}
			, #{companyId}
			, SYSDATE
		)
	</insert>
	
	<delete id="deleteScrabCompany">
		DELETE FROM SCRAB_COMPANY
		WHERE USER_ID = #{userId}
		AND COMPANY_ID = #{companyId}
	</delete>
</mapper>