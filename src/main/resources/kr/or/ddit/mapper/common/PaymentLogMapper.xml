<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.common.PaymentLogMapper">
	<select id="selectLog">
		SELECT
		LOG_ID
		, PAYMENT_NO
		, PRODUCT_NO
		, USED_COUNT
		,
		USED_AT
		, EMAIL_ADDRESS
		, SUBJECT
		, MESSAGE_BODY
		FROM PAYMENT_LOG
	</select>

	<select id="selectLogByPk">
		SELECT
		LOG_ID
		, PAYMENT_NO
		, PRODUCT_NO
		, USED_COUNT
		,
		USED_AT
		, EMAIL_ADDRESS
		, SUBJECT
		, MESSAGE_BODY
		FROM PAYMENT_LOG
		WHERE
		LOG_ID = #{logId}
	</select>

	<insert id="insertLog">
		<selectKey resultType="string" keyProperty="logId"
			order="BEFORE">
			SELECT
			'PLOG'||
			LPAD(NVL(TO_NUMBER(SUBSTR(MAX(LOG_ID), LENGTH('PLOG' ) + 1)), 0) + 1, 6, '0')
			FROM PAYMENT_LOG
		</selectKey>
		INSERT INTO PAYMENT_LOG (
		LOG_ID
		, PAYMENT_NO
		, PRODUCT_NO
		, USED_COUNT
		, USED_AT
		, EMAIL_ADDRESS
		, SUBJECT
		, MESSAGE_BODY
		) VALUES (
		#{logId}
		, #{paymentNo}
		, #{productNo}
		, #{usedCount}
		, #{usedAt}
		, #{emailAddress}
		, #{subject}
		, #{messageBody}
		)
	</insert>

	<update id="updateLog">
		UPDATE PAYMENT_LOG
		SET
		PAYMENT_NO = #{paymentNo}
		,
		PRODUCT_NO = #{productNo}
		, USED_COUNT = #{usedCount}
		, USED_AT =
		#{usedAt}
		, EMAIL_ADDRESS = #{emailAddress}
		, SUBJECT = #{subject}
		,
		MESSAGE_BODY = #{messageBody}
		WHERE LOG_ID = #{logId}
	</update>

	<delete id="deleteLog">
		DELETE FROM PAYMENT_LOG
		WHERE LOG_ID = #{logId}
	</delete>
</mapper>