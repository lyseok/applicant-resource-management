<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.common.File">
<insert id="insertFile">
		INSERT INTO FILE (
		FILE_NO
		,REAL_FILE
		,FILE_NAME
		,FILE_SIZE
		,FILE_TYPE
		) VALUES (
		 #{fileNo}
		,#{realName}
		,#{fileName}
		,#{fileSize}
		,#{fileType}
		)
	</insert>

	<select id="selectFileList">
		SELECT 
		 FILE_NO
		,REAL_FILE
		,FILE_NAME
		,FILE_SIZE
		,FILE_TYPE
		FROM FILE
	</select>

	<select id="selectFileByPk">
		SELECT 
		 FILE_NO
		,REAL_FILE
		,FILE_NAME
		,FILE_SIZE
		,FILE_TYPE
		FROM FILE
		WHERE FILE_NO = #{fileNo}
		AND FILE_NAME = #{fileName}
	</select>

	<update id="updateFile">
		UPDATE FILE
	SET 
		REAL_FILE	= #{realFile}
		,FILE_NAME	= #{fileName}
		,FILE_SIZE	= #{fileSize}
		,FILE_TYPE	= #{fileType}	
	FROM FILE	
	WHERE
		FILE_NO = #{fileNo}
	</update>

	<delete id="deleteFile">
		DELETE FROM FILE
	   WHERE FILE_NO = #{forbiddenWordNo}
	  AND WORD = #{word}
	</delete>

</mapper>