<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.common.TagMapper">
	<insert id="insertTag">
		<selectKey resultType="string" keyProperty="tagNo"
			order="BEFORE">
			SELECT
			'TAGS'||
			LPAD(NVL(TO_NUMBER(SUBSTR(MAX(TAG_NO), LENGTH('TAGS' ) + 1)), 0) + 1, 6, '0')
			FROM TAG
		</selectKey>
		INSERT INTO TAG (
		TAG_NO
		, TAG_NAME
		) VALUES (
		#{tagNo}
		, #{tagName}
		)

	</insert>
	
	<select id="selectTagNo">
		SELECT 
		    TAG_NO
		FROM TAG
		WHERE TAG_NAME = #{tagName}
	</select>

	<select id="selectTagList">
		SELECT
		TAG_NO
		, TAG_NAME
		FROM TAG
	</select>

	<select id="selectTagByPk">
		SELECT
		TAG_NO
		, TAG_NAME
		FROM TAG
		WHERE TAG_NO =
		#{tagNo}
	</select>

	<update id="updateTag">
		UPDATE TAG
		SET
		TAG_NAME = #{tagName}
		WHERE TAG_NO =
		#{tagNo}
	</update>

	<delete id="deleteTag">
		DELETE FROM TAG
		WHERE TAG_NO = #{tagNo}
	</delete>

</mapper>