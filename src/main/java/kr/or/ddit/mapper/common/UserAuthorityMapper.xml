<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mapper.common.UserAuthorityMapper">

<select id="selectUserAuthorityList">
	SELECT 
		USER_ID
		, USER_AUTHORITY
		, AUTH_NO
	FROM USER_AUTHORITY
</select>

<select id="selectUserAuthorityByPk">
	SELECT 
		USER_ID
		, USER_AUTHORITY
		, AUTH_NO
	FROM USER_AUTHORITY
	WHERE AUTH_NO = #{authNo}
</select>

<insert id="insertUserAuthority">
	INSERT INTO USER_AUTHORITY (
		USER_ID
		, USER_AUTHORITY
		, AUTH_NO
	) VALUES (
		#{userId}
		, #{userAuthority}
		, #{authNo}
	)
</insert>

<update id="updateUserAuthority">
	UPDATE USER_AUTHORITY
	SET 
		USER_ID = #{userId}
		, USER_AUTHORITY = #{userAuthority}
	WHERE AUTH_NO = #{authNo}
</update>

<delete id="deleteUserAuthority">
	DELETE FROM USER_AUTHORITY
	WHERE AUTH_NO = #{authNo}
</delete>

</mapper>